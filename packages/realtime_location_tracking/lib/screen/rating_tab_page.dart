import 'package:flutter/material.dart';

class RatingTabPage extends StatelessWidget {
  const RatingTabPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("This is Rating page"),
    );
  }
}
